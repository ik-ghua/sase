# L1 v0.6 累积回写 · 一致性体检记录

> **体检对象:** `docs/sase-architecture-design.md` v0.5 → **v0.6** 累积回写,连带 `docs/sase-gm-crypto-selection.md` v0.3 → **v0.4**、控制面 L2 总览(业务模块 6→7)及全部 L2 文档上承版本刷 v0.6。
> **体检日期:** 2026-05-27
> **执行:** `design-doc-reviewer` subagent(跨文档一致性体检,三遍)+ 花刚指示范围。
> **性质:** **这是一致性体检记录(自审),非花刚主持的 Q&A 设计评审。** 用于确认 v0.6 回写在 L1 / gm-crypto / 各 L2 之间口径自洽、无悬空引用、无新引入矛盾;**架构决策本身的方向评审仍待花刚另行进行(若需要)。**
> **触发:** 花刚"先自审一遍" → 体检揪出累积漂移 → "现在就做 L1 v0.6 回写,然后修两处矛盾" → 回写 + 三遍复检收敛。

---

## 一、体检结论

- [x] **通过**(口径一致,无硬错误,无阻塞项,可进入评审)
- [ ] 有条件通过
- [ ] 需修改后重检

**结论说明:** 三遍 design-doc-reviewer 复检收敛至**无硬错误、无阻塞项**。隧道形态、xDS 资源计数、业务模块计数、版本号、风险闭环、待确认项汇总六项跨文档口径一致;"待/须/建议回写"漂移已全 docs 树根除。**纯文档回写,未动代码,未发明架构(外科式应用已记录/已论证项)。**

---

## 二、本轮回写内容(Slices 15–25 as-built + 决策累积)

L1 v0.6 主体:

| # | 项 | 落点 |
|---|----|------|
| ① | **SD-WAN 数据面隧道形态修正**(主线):「TLCP/铜锁 TLS 流隧道」→「自研 crypto-agile UDP 数据报隧道」(SM4-GCM↔ChaCha20-Poly1305 可插拔 + FEC),握手用 TLS1.3/TLCP 导出密钥(零自研密钥协商,待密码学审查)。**国密算法栈与 C-G1 不变,变的是封装形态;ZTNA 保持 L7,管理面/控制信令面仍 TLCP。** | 3.7/R7/附录组件表/非目标 2.3 + gm-crypto v0.4(header/3.2) |
| ② | 自定义 xDS 资源 4→6 类(+`FWRuleSet`/`DLPRuleSet`) | 3.1/3.11/5.2 |
| ③ | 控制面新增第 7 业务模块 `risk`(信任/风险引擎) | 3.8 + 控制面 L2 总览(6→7) |
| ④ | 三项安全能力 SWG/FWaaS/CASB-DLP 全部 as-built(加能力不加基础设施) | 3.10/5.2 |
| ⑤ | 遥测管道(单元③)DLP→风险闭环 + W11 角色化 PKI | 3.14 |
| ⑥ | ZTP 入网/续期/撤销 + revtunnel 租户绑定 W9 | 3.5 |
| ⑦ | 审计事务化 DB 触发器设计(方案 A,待评审编码) | 3.14 + `sase-l2-audit-transactional.md` |
| ⑧ | 5.2 as-built 改写为 Slices 0–25;附录 B 新增 PoC-T(隧道握手密码学审查)+ D1(审计 2xx-零变更盲区) | 5.2 / 附录 B |

backlog `l1-v05-writeback-backlog`(W1–W11)全部回写,**关闭**。

---

## 三、三遍复检收敛过程

| 遍 | 发现 | 处置 |
|----|------|------|
| 第一遍 | H1 控制面总览 6→7、A1/B1 隧道形态漂移(L1/gm-crypto 落后于隧道 L2)、A3–A8 as-built 欠账 | 完成 L1 v0.6 主体回写 + gm-crypto v0.4 + H1/H2/H3 |
| 第二遍 | **CPE L2 隧道形态裸冲突**(整份仍 WireGuard/TLCP 双栈旧口径,未被任何修正块覆盖)+ S1–S5(版本引用/过期措辞/待办登记) | 修 CPE L2(3.5 修正块 + 结论/衔接/附录)+ 全部 L2 上承版本刷 v0.6 |
| 第三遍 | H1`(L1:429「待回写控制面 L2 总览」过期措辞,系本轮自引入)+ 全仓「待/须/建议回写」漂移 | 修 L1:429 + 8 处同类残留 + 两组「6 类」消歧注 + 两条非阻塞建议清理 |

---

## 四、最终口径一致性核对(第三遍确认)

- **隧道形态全局一致**:L1 / gm-crypto / 隧道 L2 / 握手 L2 / CPE L2 五处统一为「自研 UDP 数据报隧道 + 握手 TLS1.3/TLCP 导出」,一致声明「算法栈与 C-G1 不变」「ZTNA 保持 L7」「管理面/控制信令面仍 TLCP」;Noise 仅作"已否决原方案"出现。
- **xDS 两组「6 类」消歧清晰**:**设计资源 6**(含 `TrustBundle`/`L34RuleSet`/`WGPeerSet`,xds-server/pop-orchestration L2 口径)vs **as-built 6**(`L7PolicyBundle`/`RevocationList`/`SWGRuleSet`/`FWRuleSet`/`DLPRuleSet`/`SiteConfig`,L1 v0.6 已编码口径)——两组都恰为 6 但成员不同,已加"对账勿混"注,三处互指一致。
- **业务模块 7**:控制面总览(mermaid/模块表/两处结论)、risk L2、L1:429 一致。
- **版本自洽**:L1 v0.6 / gm-crypto v0.4 / 隧道 L2 v0.2 / 全部 L2 上承 v0.6;dated `sase-status-report-2026-05-25`(引 v0.4/v0.5)与历史评审记录按时间快照保留,合理。
- **「待/须/建议回写」漂移归零**(唯一保留 `ztna-access-hardening:234` 为真前瞻项:SPA over XDP 未落地)。
- **待确认项登记**:PoC-T / D1 已进附录 B,口径一致;R1–R7 风险闭环。

---

## 五、非阻塞遗留(评审中带过或评审后处理)

- 无硬错误、无阻塞项。第三遍 reviewer 提的两条非阻塞建议(handshake L2 同文档内 backlog 残留措辞、gm-crypto:317 措辞对齐)**已于本轮收尾清掉**。
- **真正仍待(非本轮范围):** PoC-T 隧道握手密码学审查(通过后才接 cmd 跑真密钥)、D1 审计 2xx-零变更盲区(待花刚评审 audit L2 收口)、审计事务化方案 A 待评审后编码、SM4 带加速 M-G3~M-G5(待国密 CPU)、SPA over XDP(PoC 候选,AH7)。

---

## 六、后续

- L1 v0.6 / gm-crypto v0.4 **可进入评审**(若需花刚做方向性 Q&A 评审,本记录作前置一致性体检凭据,同 2026-05-25 控制面 L2 评审的"前置体检"模式)。
- 审计事务化(方案 A)待花刚评审通过后进编码。
- 进一步编码(下一刀)待授权(设计先行约定)。
